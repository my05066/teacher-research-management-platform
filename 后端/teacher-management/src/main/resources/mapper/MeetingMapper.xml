<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tit.university.mapper.MeetingMapper">


    <select id="list" resultType="com.tit.university.pojo.Meeting">
        select m.id, m.user_id userId, m.meeting_name meetingName, m.category, m.has_report hasReport, m.report_time reportTime, m.meeting_time meetingTime, m.meeting_location meetingLocation, m.status,
        m.evidence_path evidencePath, m.evidence_name evidenceName, m.evidence_size evidenceSize, m.audit_user_id auditUserId, m.audit_time auditTime, m.audit_remark auditRemark,
        m.create_time createTime, m.update_time updateTime, m.delete_flag deleteFlag,
        u.real_name userName, u.employee_id employeeId, u.phone phone, u.department department, u.email email
        from meeting m left join users u on m.user_id = u.id where m.delete_flag = 0
        <if test="userId != null"> and m.user_id = #{userId} </if>
        <if test="status != null and status != '' "> and m.status = #{status} </if>
        order by m.create_time desc
    </select>


    <select id="countByUserId" resultType="java.lang.Long">
        select count(*) from meeting where delete_flag = 0
        <if test="userId != null"> and user_id = #{userId} </if>
        <if test="status != null and status != '' "> and status = #{status} </if>

    </select>


</mapper>
